# MkDocs build output — regenerated by `mkdocs build` and by CI (see .github/workflows/pages.yml).
# It must never be committed: it's a derived artifact, and a stale checked-in copy would drift
# from the docs it was built from.
/site/

# Python / tooling cruft from building locally
__pycache__/
*.py[cod]
.venv/
venv/
.cache/
.DS_Store
